/* exported isPalindromic */
function isPalindromic(string) {
  const reversed;
  for (var i = word.length - 1; i >= 0; i--) {
    reversed += word[i];
  }
console.log('reversed input string', reversed);
}
